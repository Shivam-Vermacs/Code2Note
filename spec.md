title — string — filename or inferred problem title (used as the Notion page title).

language — string — detected language (cpp, py, js). Helps choose syntax highlighting.

problem — string — short inferred problem statement; may be labeled “inferred”.

approach — string — a short explanation of the main idea you used.

pseudocode — string — compact pseudocode capturing the algorithm.

complexity — string — time & space complexity estimate (heuristic).

edgeCases — array[string] — likely edge cases to consider (empty, duplicates, etc).

code — string — the original source (trimmed or full; include TRUNCATED indicator if trimmed).